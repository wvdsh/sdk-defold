function init(self)



	wavedash.init(function(self, event, payload)
		print("event:", event, "payload:")
		pprint(payload)
	end)
	wavedash.ready_for_events()
	wavedash.toggle_overlay()
	wavedash.create_lobby(wavedash.LOBBY_VISIBILITY_PUBLIC, 4)
	wavedash.request_stats()
end

